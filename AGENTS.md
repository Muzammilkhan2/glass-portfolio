# AGENTS.md

Commands for this repo (run from `glass-portfolio/`):

- Lint: `npm run lint`  (runs `next lint`)
- Typecheck: `npx tsc --noEmit`
- Build (includes typecheck): `npm run build`
- Dev: `npm run dev`
